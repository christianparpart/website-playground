# Website Playground Learning Project

- tools:
  - bower
  - grunt
- frameworks:
  - bootstrap
  - jquery

```
# install required NodeJS packages
npm install

# install required Bower packageas
bower install

# start static web server
grunt connect:server

# auto-recompile assets
grunt watch
```
